// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Default DATABASE_URL for production
// DATABASE_URL="file:./dev.db"

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Post {
  id        String   @id @default(cuid())
  title     String
  content   String?
  published Boolean  @default(false)
  authorId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// AI Learning Models
model TradeRecord {
  id              String   @id @default(cuid())
  timestamp       DateTime @default(now())
  tokenSymbol     String
  tokenName       String
  tokenMint       String
  action          String   // BUY or SELL
  amount          Float
  entryPrice      Float
  exitPrice       Float?
  exitTimestamp   DateTime?
  outcome         String   @default("PENDING") // WIN, LOSS, PENDING
  profitPercent   Float?
  profit          Float?
  strategy        String
  aiScore         Float    // 0-10
  aiRecommendation String?
  marketCondition  String?
  holdDuration    Int?     // milliseconds
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([tokenSymbol])
  @@index([outcome])
  @@index([strategy])
}

model TokenPerformance {
  id              String   @id @default(cuid())
  symbol          String   @unique
  totalTrades     Int      @default(0)
  wins            Int      @default(0)
  losses          Int      @default(0)
  totalProfit     Float    @default(0)
  avgProfitPercent Float   @default(0)
  winRate         Float    @default(0)
  bestTrade       Float    @default(0)
  worstTrade      Float    @default(0)
  avgHoldDuration Int      @default(0)
  isBlacklisted   Boolean  @default(false)
  lastTradeAt     DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([symbol])
  @@index([isBlacklisted])
}

model StrategyPerformance {
  id          String   @id @default(cuid())
  name        String   @unique
  totalTrades Int      @default(0)
  wins        Int      @default(0)
  totalProfit Float    @default(0)
  winRate     Float    @default(0)
  avgProfit   Float    @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([name])
}

model TradingPattern {
  id              String   @id @default(cuid())
  aiScoreMin      Int
  aiScoreMax      Int
  timeOfDay       Int      // 0-23 hour
  sentiment       String   @default("NEUTRAL")
  outcome         String   @default("PENDING")
  totalProfit     Float    @default(0)
  occurrenceCount Int      @default(1)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([aiScoreMin, aiScoreMax])
  @@index([timeOfDay])
}

model LearningConfig {
  id                      String   @id @default(cuid())
  key                     String   @unique
  value                   String
  description             String?
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt
}

// Active Positions (for persistence)
model Position {
  id            String   @id @default(cuid())
  symbol        String
  name          String
  mint          String
  entryPrice    Float
  currentPrice  Float    @default(0)
  amount        Float
  valueSOL      Float
  stopLoss      Float
  takeProfit    Float
  entryTime     DateTime @default(now())
  pnl           Float    @default(0)
  pnlPercent    Float    @default(0)
  aiConfidence  Float
  aiReasoning   String?
  txSignature   String?
  status        String   @default("OPEN") // OPEN, CLOSED
  closedAt      DateTime?
  closeReason   String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  @@index([symbol])
  @@index([status])
}

// Trading Stats
model DailyStats {
  id        String   @id @default(cuid())
  date      String   @unique
  trades    Int      @default(0)
  wins      Int      @default(0)
  losses    Int      @default(0)
  pnl       Float    @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}